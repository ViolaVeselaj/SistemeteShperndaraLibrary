package com.example.sistemeteshperndara.repository;

import com.example.sistemeteshperndara.model.BookExpirationAlert;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BookExpirationAlertRepository extends JpaRepository<BookExpirationAlert, Long> {
}
